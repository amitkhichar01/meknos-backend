export const DEFAULT_TEMPERATURE = {
  MARKDOWN: 0.3,
  SUGGESTIONS: 0.7,
  CHAT: 0.4,
} as const;

export const DEFAULT_MAX_TOKENS = {
  MARKDOWN: 2048,
  SUGGESTIONS: 512,
  CHAT: 1024,
} as const;
